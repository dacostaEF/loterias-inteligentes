[build]
builder = "dockerfile"

[deploy]
# startCommand é ignorado quando há Dockerfile, mas mantenho por documentação
startCommand = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --threads 4 --timeout 120"
healthcheckPath = "/healthz"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[environments.production]
SECRET_KEY = "loterias-inteligentes-secret-key-2024-production"
SITE_CNPJ = "00.000.000/0000-00"
SITE_WHATS_LINK = "https://wa.me/55SEUNUMERO"
FLASK_ENV = "production"
FLASK_DEBUG = "False"
DOMAIN = "loteriasinteligente.com.br"
